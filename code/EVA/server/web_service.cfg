RootConfigFilename = "common.cfg";


AESAliasName = "WSE";
SId          =  60;

WebPort   = 30002;
StartCommands +=
{
	// create the login service module and open the web interface
	"moduleManager.createModule WebModules ls webPort="+WebPort,
};
