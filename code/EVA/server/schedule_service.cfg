RootConfigFilename = "common.cfg";

DisplayedVariables += { "", "@LogicService|pls" };
//GraphVars += {	"TotalConcurentUser", "60000" };

AESAliasName = "SSE";

SId            =  65;

MaxPlayer      =  50000000;

Paths =
{
	"./json_table/",              		   		// for static data
	"./i18n/"									// for il8n data
};

WebPort   = 30002;

StartCommands +=
{
	// create the login service module and open the web interface
	"moduleManager.createModule PayModule ls webPort="+WebPort,
};
